#write a function minutes to seconds(minutes)
# that coverts a given number of minutes into second

def minutes_to_seconds(minutes):
    return f"{minutes * 60}"
print(minutes_to_seconds(5))
# output:300
